# mini-rag-local

Локальный RAG по файлам из папки `data/`.

## Быстрый старт

```bash
git clone <url>  # опционально, если будет удалённый репозиторий
cd mini-rag-local
python -m venv .venv
source .venv/bin/activate  # или .\.venv\Scripts\activate на Windows
pip install -r requirements.txt
