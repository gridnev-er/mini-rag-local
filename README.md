# mini-rag-local
 
Идея:

> Берём файлы из папки `data/` → режем на чанки → считаем эмбеддинги → строим FAISS-индекс →  
> отвечаем на вопросы, опираясь только на эти документы, с указанием источников.

---

# Возможности

- 🔎 Поиск по локальным документам (`.txt`, `.md`, текстовые `.pdf`).
- 🧩 Чанкинг текстов с overlap (по умолчанию 1000 символов + 150 перекрытия).
- 📐 Эмбеддинги через `sentence-transformers/all-MiniLM-L6-v2`.
- 📚 Векторный индекс на FAISS (`IndexFlatIP`).
- 🤖 Генерация ответа через OpenAI (`gpt-4o-mini` по умолчанию).
- 📎 Раздел «Источники» — для каждого ответа показываются файлы и сниппеты, на которые он опирается.
- 💻 Два режима работы:
  - CLI: `python app.py --ask "..."`;
  - Web UI на Gradio: `python app.py --ui`.

---

# Требования

- Python **3.10+**
- Установленный `git`
- Учётная запись OpenAI и API-ключ

---

# Установка и первый запуск

## Клонируем репозиторий

```bash
git clone https://github.com/gridnev-er/mini-rag-local.git
cd mini-rag-local
```

## Создаём и активируем виртуальное окружение

#### macOS / Linux:

```bash
python -m venv .venv
source .venv/bin/activate
```

#### Windows (PowerShell):
```bash
python -m venv .venv
.\.venv\Scripts\activate
```

## Устанавливаем зависимости

```bash
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

## Настраиваем ключ OpenAI

#### Создаём .env на основе примера:

```bash
cp .env.example .env
```

#### Открываем .env и прописываем реальный ключ:

```bash
OPENAI_API_KEY=sk-...
```

⚠️ Файл .env уже добавлен в .gitignore, в репозиторий его коммитить не нужно.


## Подготовка документов

Положите свои файлы в папку data/

#### поддерживаются:

```bash
.txt
.md
текстовые .pdf (без OCR)
```

#### игнорируются:

```bash
скрытые файлы (.DS_Store, .git, и т.п.)
форматы, которые не умеем парсить
```

## Сборка индекса

#### После того как документы лежат в data/, нужно собрать индекс:

```bash
python index_build.py
```

#### Что делает этот скрипт:
- Загружает и нормализует документы (rag.io_loaders).  
- Режет тексты на чанки (rag.chunker).  
- Считает эмбеддинги для каждого чанка (rag.embeddings).  
- Строит FAISS-индекс по всем эмбеддингам (rag.index_store).  

#### Сохраняет:

- Бинарный индекс в index/.  
- Метаданные чанков в index/chunks.jsonl.  


## Режимы работы

### 1. CLI: вопрос из терминала

```bash
python app.py --ask "дай краткое определение: что такое RAG?"
```

#### Что происходит:

- Загружается конфиг и индекс.
- Считается эмбеддинг вопроса.
- Ретривер выбирает top_k релевантных чанков.
- LLM формирует ответ.
- В конце выводятся «Источники» — файлы и сниппеты.

### 2. Web UI: Gradio

```bash
python app.py --ui
```

В консоли появится что-то вроде:  
[APP] Запуск веб-интерфейса Gradio...  
Running on local URL:  http://127.0.0.1:7860

#### Откройте указанный URL в браузере.

